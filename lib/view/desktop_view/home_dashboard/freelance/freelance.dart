import 'package:flutter/material.dart';

class Freelances extends StatelessWidget {
  const Freelances({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
